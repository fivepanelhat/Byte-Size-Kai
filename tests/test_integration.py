"""Public tests disabled — commercial-track / private."""

import pytest

pytest.skip(
    "Byte Size Kai: public tests are disabled. Method packs are commercial-track / private. See README.md.",
    allow_module_level=True,
)
